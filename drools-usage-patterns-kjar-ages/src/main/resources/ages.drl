package com.rhc.drools.example
import com.rhc.drools.example.persistence.MyPerson;
import com.rhc.drools.example.persistence.MyName;

rule "Sal's age"
when
	$sal : Person(age != 55, name.getFirstName() == "Sal")
then
	$sal.setAge(55);
end

rule "Sam's age"
when
    $sam : Person(name.firstName == "Sam", name.lastName == "Rang")
then
    $sam.setAge(28);
end
